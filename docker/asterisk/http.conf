; ============================================================
; LNAYCRM — http.conf (Asterisk ARI + WebSocket)
; ============================================================

[general]
enabled         = yes
bindaddr        = 0.0.0.0
bindport        = 8088
prefix          = asterisk
sessionlimit    = 100
