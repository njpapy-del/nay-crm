; ============================================================
; LNAYCRM — ari.conf
; Asterisk REST Interface — utilisé par NestJS
; ============================================================

[general]
enabled = yes
pretty  = yes          ; JSON lisible (désactiver en prod)
allowed_origins = http://localhost:3000,https://lnaycrm.yourdomain.com

; Compte NestJS backend
[lnaycrm-backend]
type     = user
read_only = no
password = ARI_LNAYCRM_SECRET_2024!   ; changer en production
